import React from "react";

type StoreId = {
    id: string;
};

export default function StoreMap({ id }: StoreId): JSX.Element {
    return (
        <>
            <div>
                <h3>StoreMap</h3>
            </div>
        </>
    );
}
