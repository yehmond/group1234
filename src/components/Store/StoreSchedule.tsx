import React from "react";

type StoreId = {
    id: string;
};

export default function StoreSchedule({ id }: StoreId): JSX.Element {
    return (
        <>
            <div>
                <h3>SCHEDULE TODO</h3>
            </div>
        </>
    );
}
